# browse-agent

<!--
## 1. Золотая середина (Надежность OpenAI, но дешевле)

- **Модель:** `gpt-5.4-mini`
- **Цена:** $0.75 (вход) / $4.50 (выход) за 1М токенов.
- **Почему стоит взять:** Это прямое развитие линейки компактных моделей OpenAI (наследник `gpt-4o-mini`). Она специально оптимизирована для частых вызовов, быстро работает и **идеально поддерживает Tool Calling и JSON-форматирование**. Она дешевле `gpt-4o` более чем в 3 раза на вход и в 2 раза на выход.

## 2. Максимальная экономия при топовом интеллекте

- **Модель:** `deepseek-v4-pro`
- **Цена:** $0.464 (вход) / $0.928 (выход) за 1М токенов.
- **Почему стоит взять:** DeepSeek сейчас — это один из лидеров рынка по соотношению цена/качество. V4 Pro обладает огромным контекстом, великолепно пишет код, анализирует текст и отлично работает с вызовом функций. За такую цену (почти в 10 раз дешевле `gpt-4o`) это настоящий подарок для агентов.
- _Опционально:_ `deepseek-v4-flash` — еще дешевле ($0.154 / $0.308), отлично подойдет для простых задач, вроде классификации отзывов.

## 3. Лучшая работа с нестандартными форматами и мульти-шаговыми задачами

- **Модель:** `qwen3.7-plus`
- **Цена:** $0.282 (вход) / $1.128 (выход) за 1М токенов.
- **Почему стоит взять:** Модели серии Qwen (от Alibaba) невероятно хороши в Agent-задачах. Они нативно умеют пользоваться инструментами, отлично понимают русский язык и стоят копейки. Версия `Plus` — это отличный компромисс между скоростью и мощностью.

## 4. Для быстрых агентских циклов (Google)

- **Модель:** `gemini-3.5-flash`
- **Цена:** $1.50 (вход) / $9.00 (выход) за 1М токенов.
- **Почему стоит взять:** В описании прямо указано: _"Designed for the agentic era, it excels at sub-agent deployment, multi-step workflows"_. Она чуть дешевле `gpt-4o`, но работает молниеносно. Подойдет, если вам важна скорость.

## 5. Для тестов и разработки (Бесплатно)

- **Модель:** `gpt-5.5-free` или `step-3.7-flash-free`
- **Цена:** $0.00 / $0.00
- **Почему стоит взять:** Пока вы отлаживаете промпты или тестируете новые тулзы, логично использовать бесплатные версии (у них есть лимит — 5 запросов в минуту, но для тестов этого обычно хватает).
 -->
