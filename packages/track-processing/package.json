{
  "name": "@injurka/track-processing",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "description": "GPS track processing: noise filters, RDP simplification, Catmull-Rom spline, activity classification (walk/bike/vehicle/rail)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "engines": {
    "bun": ">=1.4.0"
  },
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "typecheck": "bunx tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@antfu/eslint-config": "9.5.1",
    "eslint": "^10.10.0",
    "eslint-plugin-format": "2.0.1",
    "typescript": "6.0.3",
    "vitest": "^5.0.0"
  }
}
